{
  "name": "faster",
  "version": "1.0.0",
  "devDependencies": {
    "nodemon": "^3.1.9",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2",
    "web-ext": "^8.5.0"
  },
  "scripts": {
    "watch": "nodemon --watch --ext ts --exec \"npm run format && npx tsc --watch\"",
    "format": "prettier --write \"src/**/*.ts\"",
    "package": "web-ext build",
    "dev": "web-ext run"
  }
}
