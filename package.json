{
  "name": "faster",
  "version": "1.0.0",
  "devDependencies": {
    "nodemon": "^3.1.9",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "watch": "nodemon --watch --ext ts --exec \"npm run format && npx tsc --watch\"",
    "format": "prettier --write \"src/**/*.ts\"",
    "run:extension": "web-ext run"
  }
}
